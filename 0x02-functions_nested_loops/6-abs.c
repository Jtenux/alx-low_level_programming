#include "main.h"
/**
 * _abs - this computes the absolut value of an integer
 * @i: integer to check
 * Return: absolute value of integer
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
		return (-i);
}

