#include <stdio.h>
/**
 * main - prints natural numbers
 *
 * Return: always 0 when succesful
 */
int main(void)
{
	int m;
	int n = 0;

	while (m < 1024)
	{
		if ((m % == 0) || (m % 5 == 0))
		{
			n += m;
		}
		m++
	}
	printf("%d\n", n);
	return (0);
}
